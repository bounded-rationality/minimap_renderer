# Core renderer (install from original repo)
# pip install git+https://github.com/WoWs-Builder-Team/minimap_renderer.git

# Additional dependencies not included in the renderer's requirements
langdetect
hanzidentifier
