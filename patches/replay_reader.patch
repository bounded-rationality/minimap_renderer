# PATCH: replay_unpack/replay_reader.py
#
# Change required: In the get_replay_data() method, add a check to skip
# zero-size blocks introduced in the WoWs 15.x replay format.
#
# Find this code:
#
#     extra_data = []
#     for i in range(blocks_count - 1):
#         block_size = struct.unpack("i", f.read(4))[0]
#         data = json.loads(f.read(block_size))
#         extra_data.append(data)
#
# Replace with:
#
#     extra_data = []
#     for i in range(blocks_count - 1):
#         block_size = struct.unpack("i", f.read(4))[0]
#         if block_size == 0:
#             continue
#         data = json.loads(f.read(block_size))
#         extra_data.append(data)
#
# This patch is applied automatically by apply_patches.py
